name: evna_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.1+1

environment:
  sdk: ">=2.12.0 <3.0.0"
  # sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  provider: ^5.0.0 # for State management
  flutter_launcher_icons: ^0.9.0 # change app icon
  get_it: ^7.2.0
  json_annotation: ^4.0.1
  http_auth: ^1.0.0
  freezed: ^0.14.2
  freezed_annotation: ^0.14.2
  flutter_bloc: ^7.0.0
  flutter_slidable: ^0.6.0 #for slidable cards
  persistent_bottom_nav_bar: ^4.0.2
  connectivity: ^3.0.3
  fluttertoast: ^8.0.3
  uuid: ^3.0.4
  synchronized: ^3.0.0
  # smart_select: ^4.3.2
  timeago: ^3.1.0
  image_picker: ^0.8.3+2
  intl: ^0.17.0
  share_plus: ^2.1.4
  moor_flutter: ^4.0.0
  sqflite: ^2.0.0+3
  # geocoder: ^0.2.1
  address_search_field: ^4.0.1

  get: ^4.3.8

  shared_preferences: ^2.0.0

  google_maps_flutter: ^2.0.6

  firebase_core: ^1.0.2
  firebase_auth: ^3.0.2
  # firebase_messaging: ^9.1.0
  firebase_analytics: ^8.2.0
  firebase_storage: ^10.0.2
  cloud_firestore: ^2.5.0
  google_sign_in: ^5.0.1
  flutter_facebook_auth: ^3.5.0+1
  flutter_facebook_auth_web: ^2.6.0+2
  # flutter_facebook_auth: ^3.3.4
  # firebase_auth_web: ^1.0.4
  firebase_auth_web: ^3.0.1
  sign_in_with_apple: ^3.0.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  agora_rtc_engine: ^4.0.6
  permission_handler: ^8.0.0

  sound_stream: ^0.3.0
  rxdart: ^0.27.1
  dialogflow_grpc: ^0.2.9
  #Country Code Picker
  country_code_picker:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.1
  json_serializable: ^4.1.4
  flutter_native_splash: ^1.1.7+1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/EvnaIcon.png"

flutter:
  uses-material-design: true

  fonts:
    - family: Poppins
      fonts:
        - asset: "assets/fonts/poppins/Poppins-Medium.ttf"
        - asset: "assets/fonts/poppins/Poppins-MediumItalic.ttf"
          # style: italic

  assets:
    - assets/images/
    - assets/json/
